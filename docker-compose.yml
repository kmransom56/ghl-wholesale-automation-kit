services:
  ghl-automation:
    build: .
    container_name: ghl-wholesale-automation
    ports:
      - "${SERVER_PORT:-5000}:5000"
    env_file:
      - .env
    environment:
      FLASK_ENV: production
      SERVER_PORT: "5000"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
